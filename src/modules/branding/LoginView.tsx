// RSC wrapper for the login view — currently a placeholder
// for Phase 2+ when we add a full custom login page replacement.
// For now, branding is handled via beforeLogin injection (LoginBackground).

export { LoginBackground as LoginView } from './LoginBackground.js'
